# Tech Debt Catalog

Automated catalog generated by `maestro adapt`. Items are ordered by severity.
Status updated as of v0.13.1 milestone.

## High

- [x] **[PoorAbstractions]** `src/tui/app/` — App struct accumulated too many responsibilities. **Fix:** Extracted CI polling into `CiPoller` struct (#363) and session spawning into standalone factory functions (#364). Further extraction possible.
- [x] **[SecurityConcern]** `maestro-state.json` and runtime files — Runtime state files were not gitignored. **Fix:** Already resolved — `.gitignore` entries present (#352).
- [x] **[InconsistentPatterns]** `src/github/` vs `src/provider/github/` — Dual GitHub module locations. **Fix:** Consolidated all GitHub code under `src/provider/github/` (#355).
- [x] **[MissingTests]** `src/adapt/` — Multi-stage pipeline lacked unit test coverage. **Fix:** Added planner, analyzer, and integration tests (#358).
- [x] **[MissingTests]** `src/review/` — Review council and dispatch lacked test coverage. **Fix:** Added council aggregation and dispatch routing tests (#359).

## Medium

- [x] **[Documentation]** `src/turboquant/` — Domain-specific module lacked documentation. **Fix:** Added `//!` module-level docs to all sub-modules (#361).
- [x] **[InconsistentPatterns]** `src/provider/azure_devops.rs` vs `src/provider/azure_devops/` — Dual file/directory form. **Fix:** Consolidated to directory form (#356).
- [x] **[MissingTests]** `src/tui/snapshot_tests/snapshots/` — No CI enforcement for snapshot diffs. **Fix:** Added `INSTA_UPDATE=no` CI step (#360).
- [x] **[PoorAbstractions]** `src/flags/` vs `src/state/` — Undocumented boundary between persistence modules. **Fix:** Added module-level docs explaining the intentional separation (#357).
- [x] **[Documentation]** `build.rs` — Build script lacked documentation. **Fix:** Added `//!` doc comment and `cargo:rerun-if-changed` directives (#353).

## Low

- [x] **[DeadCode]** `src/modes/mod.rs` — Stub module with no docs. **Fix:** Scaffolded with module-level documentation; narrowed `#[allow(dead_code)]` (#354).
- [x] **[PoorAbstractions]** `src/util.rs` — Flat utility grab-bag. **Fix:** Split into `src/util/formatting.rs` and `src/util/validation.rs` (#362).

---
*Generated by `maestro adapt` on 2026-04-16. Updated 2026-04-17 after v0.13.1 implementation.*
